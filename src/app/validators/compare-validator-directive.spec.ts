import { CompareValidatorDirective } from './compare-validator-directive';

describe('CompareValidatorDirective', () => {
  it('should create an instance', () => {
    expect(new CompareValidatorDirective()).toBeTruthy();
  });
});

import { ChangeCoordinatesValueDto } from './change-coordinates-value-dto';

describe('ChangeCoordinatesValueDto', () => {
  it('should create an instance', () => {
    expect(new ChangeCoordinatesValueDto()).toBeTruthy();
  });
});
